import { StyleSheet } from 'react-native';

export const screenView = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });

  export const introLogo = StyleSheet.create({
    container: { 
      width: 110,
      height: 106,
    },
  });